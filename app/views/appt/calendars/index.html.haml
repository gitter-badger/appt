- title Appt::Calendar.model_name.human(count: 2)
- breadcrumbs

.container
  = render partial: 'table', locals: { calendars: @calendars }
  = link_to 'Create Calendar', new_calendar_path, class: 'btn btn-sm btn-default'
